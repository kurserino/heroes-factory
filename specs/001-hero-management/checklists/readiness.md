# Implementation Readiness Checklist: Hero Management

**Purpose**: Validate the requirements-writing quality across spec.md, plan.md, data-model.md, and contracts/heroes-api.md before implementation — covering full hero-management coverage, activation/deletion separation, hard-deletion semantics, inactive-hero restrictions, REST/response consistency, pagination, search, frontend UX states, feedback, confirmation flows, validation, Docker/MySQL reproducibility, migrations, backend and frontend test coverage, documentation, and architectural proportionality.
**Created**: 2026-08-14
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [data-model.md](../data-model.md) · [contracts/heroes-api.md](../contracts/heroes-api.md)

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Coverage of Hero Management Requirements

- [ ] CHK001 Are requirements defined for every stated lifecycle capability (create, browse, search, inspect, edit, activate, deactivate, delete)? [Completeness, Spec §User Scenarios]
- [ ] CHK002 Is the full hero field set (id, name, nickname, date_of_birth, universe, main_power, avatar_url, is_active, created_at, updated_at) enumerated exactly once as the canonical representation, with no alternate or conflicting field list elsewhere in the spec/plan/data-model? [Consistency, Spec §FR-023, Data Model §Entity: Hero]
- [ ] CHK003 Are requirements traceable from each acceptance scenario in spec.md to at least one functional requirement (FR-###)? [Traceability]
- [ ] CHK004 Is there a requirement covering what happens to a hero record after every possible lifecycle action (create → active; edit while active; deactivate; reactivate; delete while active), with no unstated transition left implicit? [Coverage, Data Model §State transitions]

## Activation State vs. Deletion Separation

- [ ] CHK005 Does the spec explicitly state that activation state and deletion are independent concepts, rather than leaving this inferable only from separate FRs? [Clarity, Spec §Hero lifecycle Item 4]
- [ ] CHK006 Are the requirements for "deactivate" and "delete" written so that no single sentence conflates the two operations or their outcomes? [Consistency, Spec §FR-008, FR-010]
- [ ] CHK007 Is it specified which operation(s) are reversible (reactivation) versus irreversible (permanent deletion), for every state a hero can be in? [Clarity, Spec §Hero lifecycle]
- [ ] CHK008 Are there requirements preventing deletion from being expressed as a special case of deactivation (or vice versa) anywhere in the data model or API contract? [Consistency, Data Model §Validation rules, Contract §DELETE /heroes/:id]

## Hard Deletion Semantics

- [ ] CHK009 Does the spec explicitly prohibit a `deleted_at` field or any other persisted deletion-state marker, rather than only implying it by omission? [Clarity, Spec §FR-023]
- [ ] CHK010 Is "permanently removes" defined precisely enough (e.g., "no longer retrievable through any listing, search, or lookup") to be objectively verifiable? [Measurability, Spec §FR-024]
- [ ] CHK011 Are the requirements for delete responses (e.g., 404 on subsequent GET) specified for the hard-deletion case, distinguishing it from a hypothetical soft-delete visibility rule? [Completeness, Contract §DELETE /heroes/:id, §GET /heroes/:id]
- [ ] CHK012 Is the data model's persistence mapping free of any soft-deletion column, index, or migration step? [Consistency, Data Model §Persistence mapping]

## Inactive Hero Restrictions

- [ ] CHK013 Are the exact set of actions denied to an inactive hero (edit, delete) and the exact set still permitted (view, reactivate) both stated unambiguously in one place? [Clarity, Spec §Hero lifecycle Item 3]
- [ ] CHK014 Is the required visual treatment for inactive heroes ("gray appearance") specific enough to be a testable requirement, or does it need quantification (e.g., which elements gray out)? [Ambiguity, Spec §FR-013]
- [ ] CHK015 Are server-side rejection requirements for edit/delete-on-inactive specified with an exact status code and error condition, rather than left to implementation discretion? [Measurability, Contract §PATCH /heroes/:id, §DELETE /heroes/:id]
- [ ] CHK016 Does the spec address the race condition where a hero becomes inactive between the client loading the list and the client acting on it? [Edge Case, Spec §Edge Cases]
- [ ] CHK017 Is it specified whether the status-change endpoint remains available on an inactive hero, distinguishing it from the edit/delete restrictions? [Consistency, Contract §PATCH /heroes/:id/status]

## REST API & Response Representation Consistency

- [ ] CHK018 Are HTTP methods and paths specified consistently for every hero operation (create/list/retrieve/edit/status/delete), with no operation left without a defined method+path? [Completeness, Contract §heroes-api.md]
- [ ] CHK019 Is the success/error status code specified for every endpoint × outcome combination (e.g., edit-on-inactive, delete-on-inactive, not-found, validation failure)? [Completeness, Contract §heroes-api.md]
- [ ] CHK020 Is the hero JSON representation defined once as a canonical shape and referenced (not redefined with variations) by every endpoint that returns a hero? [Consistency, Contract §Hero representation]
- [ ] CHK021 Is the error response shape defined consistently across all endpoints, including which fields are guaranteed present for every error type? [Consistency, Contract §Error representation]
- [ ] CHK022 Is there a requirement that implementation-layer errors (ORM/database/framework internals) must never appear in a client-facing error response? [Coverage, Contract §Error representation]
- [ ] CHK023 Are field name conventions (e.g., snake_case in the API body vs. any internal camelCase) documented so frontend and backend requirements do not silently disagree? [Consistency, Ambiguity]

## Pagination Requirements

- [ ] CHK024 Is the page size requirement stated as an exact, non-configurable value (10) rather than a default that could be overridden? [Clarity, Spec §Frontend behavior Item 3]
- [ ] CHK025 Are the pagination metadata fields returned by the list endpoint fully enumerated (e.g., page, limit, total, totalPages)? [Completeness, Contract §GET /heroes]
- [ ] CHK026 Is the ordering rule (created_at descending) specified together with a tiebreak rule for equal timestamps, so the ordering is deterministic? [Completeness, Data Model §Ordering & pagination]
- [ ] CHK027 Is the requirement for automatic page-back navigation, when an operation empties the current page, precise about which page the user lands on and that it is re-fetched? [Clarity, Spec §Clarifications Session 2026-08-14]
- [ ] CHK028 Are pagination behavior requirements identical for the unfiltered list and search-filtered results, with no divergent rule stated for either? [Consistency, Spec §FR-004]

## Name/Nickname Search Requirements

- [ ] CHK029 Is the search-matching algorithm (case-insensitive substring, OR across name/nickname) stated as a requirement rather than only as an assumption? [Clarity, Spec §Assumptions]
- [ ] CHK030 Is the search trigger mechanism (explicit submission, not live-as-you-type) specified precisely enough to eliminate ambiguity about debouncing or auto-search? [Clarity, Spec §Clarifications Session 2026-08-14]
- [ ] CHK031 Is the requirement that changing the search term resets pagination to page 1 explicitly stated? [Completeness, Plan §Frontend lifecycle]
- [ ] CHK032 Is the "no results" state requirement distinguished clearly from the "empty list" (zero heroes exist) state, with different expected UI outcomes for each? [Consistency, Spec §FR-020]

## Frontend Loading State Requirements

- [ ] CHK033 Is "visible loading feedback" required for every distinct asynchronous operation (initial list load, page change, search, create, edit, delete, status toggle), with none left unaddressed? [Completeness, Spec §FR-017]
- [ ] CHK034 Is it specified whether loading feedback is scoped per-action (e.g., a single card) or global, to avoid ambiguous UI requirements? [Ambiguity, Spec §FR-017]

## Frontend Empty & Error State Requirements

- [ ] CHK035 Are the three distinct interface states (empty list, no search results, API failure) each given a distinguishing requirement rather than a single generic "show an error" statement? [Clarity, Spec §FR-020]
- [ ] CHK036 Is there a requirement for what information an API-failure state must present to the user (e.g., that it is an error, not silently empty data)? [Completeness, Spec §Edge Cases]

## Mutation Success/Error Feedback Requirements

- [ ] CHK037 Is "clear success or error feedback" required for every mutation (create, edit, activate, deactivate, delete) individually, rather than as one blanket statement whose scope is ambiguous? [Completeness, Spec §FR-018]
- [ ] CHK038 Is it specified that failed submissions must preserve entered form values, and does this requirement cover both create and edit forms equally? [Consistency, Spec §FR-019]
- [ ] CHK039 Are success criteria for feedback measurable (e.g., SC-003's "100% of attempts result in visible confirmation or specific error") rather than subjective? [Measurability, Spec §SC-003]

## Confirmation Flow Requirements

- [ ] CHK040 Is a confirmation step required before every irreversible or state-changing action (delete, activate, deactivate) with no action of that class left unconfirmed? [Completeness, Spec §FR-011, FR-012]
- [ ] CHK041 Is the requirement for blocking duplicate submissions during a pending confirmation/operation stated for all state-changing operations uniformly, not just some? [Consistency, Spec §FR-015]
- [ ] CHK042 Is modal-dismissal behavior during a pending submission specified precisely (blocked while pending, freely dismissible once idle, no confirmation required for unsaved edits)? [Clarity, Spec §FR-015a]

## Validation Requirements

- [ ] CHK043 Are field-level validation rules (required, length bounds, date not in the future) specified for every hero field accepted on create/edit, with none left undefined? [Completeness, Data Model §Validation rules]
- [ ] CHK044 Is the avatar URL validation requirement specific about what "resolves to a loadable image" means operationally (e.g., success response + image content type) so it is objectively verifiable? [Measurability, Spec §FR-025, Research §Avatar URL image verification]
- [ ] CHK045 Is it explicit that frontend validation alone is insufficient and that every validation rule must also be enforced server-side? [Clarity, Spec §FR-005/Constitution Principle V]
- [ ] CHK046 Are validation error requirements consistent between the create endpoint and the edit endpoint (same field rules, same error shape)? [Consistency, Contract §POST /heroes, §PATCH /heroes/:id]

## Docker/MySQL Reproducibility Requirements

- [ ] CHK047 Are the required docker-compose.yml capabilities (persistent volume, configurable db name/user/password, exposed port, health check) each individually specified rather than bundled into one vague statement? [Completeness, Plan §Infrastructure instructions]
- [ ] CHK048 Is it specified that frontend and backend run locally via Node.js while only the database runs in Docker, avoiding ambiguity about what is/isn't containerized? [Clarity, Plan §Database]
- [ ] CHK049 Is there a requirement that `.env.example` files exist and stay in sync with required variables, without ever committing actual secrets? [Completeness, Constitution Principle XII]

## Database Migration Requirements

- [ ] CHK050 Is it required that all schema changes go through version-controlled Prisma migrations, with manual schema edits explicitly excluded? [Clarity, Constitution Principle VII]
- [ ] CHK051 Is the requirement for consistent timestamp management (created_at set once, updated_at refreshed on every mutation including status changes) stated as a persistence-layer responsibility rather than left to each service method? [Consistency, Data Model §Persistence mapping]

## Backend Unit & Integration Test Requirements

- [ ] CHK052 Are backend unit-test priorities (creation behavior, inactive-edit rejection, inactive-delete rejection, activation, deactivation) each traceable to a specific business rule in the spec? [Traceability, Plan §Testing]
- [ ] CHK053 Are backend integration-test priorities (creation, listing, ordering, search, pagination, update, status change, hard deletion, validation, error responses) collectively covering every endpoint in the API contract? [Coverage, Contract §heroes-api.md]
- [ ] CHK054 Is there a requirement distinguishing what must be verified at the unit level (business rules in isolation) from what must be verified at the integration level (full HTTP behavior), to avoid duplicated or gapped effort? [Clarity, Constitution Principle VIII]

## Frontend Interaction Test Requirements

- [ ] CHK055 Do the frontend test priorities cover every distinct UI state identified elsewhere in the spec (loading, error, empty, no-results), with none omitted? [Consistency, Plan §Testing vs Spec §FR-020]
- [ ] CHK056 Are frontend test priorities for inactive-hero presentation and unavailable Edit/Delete actions specific enough to be written as concrete test assertions? [Measurability, Plan §Testing]
- [ ] CHK057 Are confirmation-flow and mutation-feedback test priorities each explicitly listed, rather than assumed to be implied by "form validation" or "list rendering" items? [Completeness, Plan §Testing]

## Documentation Requirements

- [ ] CHK058 Does the required README scope (architecture, folder structure, decisions, setup, migrations, startup, tests, API overview, trade-offs, future improvements) cover every artifact a new contributor needs to reach a running environment unassisted? [Completeness, Plan instructions §Documentation, Constitution Principle XIII]
- [ ] CHK059 Is there a requirement that documented architectural decisions be explainable/defensible, tying documentation quality back to a stated project goal rather than being a generic ask? [Traceability, Constitution Principle XVI]

## Architectural Proportionality & Scope Boundaries

- [ ] CHK060 Does the plan explicitly list the patterns/technologies excluded (microservices, CQRS, event sourcing, Redux, React Router, Turborepo/Nx, extra layers) so scope creep has a clear boundary to be checked against? [Clarity, Plan §Constitution Check, Constitution Principle I]
- [ ] CHK061 Is the one sanctioned abstraction (repository interface over Prisma) justified with a concrete, stated reason (unit-testability) rather than asserted without justification? [Clarity, Plan §Constitution Check Principle III, Research §Backend persistence access]
- [ ] CHK062 Is there a requirement or review gate ensuring new dependencies/abstractions introduced during implementation are checked against the "concrete, present need" standard before being added? [Gap, Constitution Principle XI]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
