# Copy this file to .env and adjust values as needed; .env is gitignored.
# Credentials below must match the root docker-compose.yml / .env values.

DATABASE_URL="mysql://heroes_app:heroes_app_password@localhost:3306/heroes"
PORT=3000
